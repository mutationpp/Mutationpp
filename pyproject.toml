[build-system]
requires = ["scikit-build-core", "setuptools-scm", "pybind11"]
build-backend = "scikit_build_core.build"

[project]
name = "mutationpp"
dynamic = ["version"]

[tool.setuptools_scm]

[tool.pytest.ini_options]
testpaths = ["tests/python"]
